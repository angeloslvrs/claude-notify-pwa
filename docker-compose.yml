services:
  claude-notif:
    build: .
    ports:
      - "7392:7392"
    volumes:
      - ./data:/app/data
    environment:
      - VAPID_SUBJECT=mailto:you@example.com
      - ADMIN_KEY=your-secret-admin-key
    restart: unless-stopped
